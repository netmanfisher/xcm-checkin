# 小虾米每日打卡

一个为小学生设计的每日学习任务打卡应用，支持多孩子管理、家长审核、奖励系统等功能。

## 功能特性

### 孩子端
- 👦 多孩子选择登录
- 📅 周历视图查看本周进度
- ✅ 每日任务打卡（支持照片、语音）
- 🏆 成就墙展示
- 🎁 愿望清单兑换
- 📊 星星获得/消费历史
- 📈 月度学习报告
- 🎨 公主主题（粉色）/王子主题（蓝色）

### 家长端
- 🔐 管理员登录
- 📋 打卡审核（通过/拒绝）
- 👶 孩子管理（添加/编辑/删除）
- 📝 学习计划管理
- 🎁 愿望管理

### 技术特点
- ⚡ Vue 3 + Vite
- 🗄️ Supabase 后端
- 💾 本地缓存 + 云端同步
- 📱 响应式设计（移动端友好）
- 🎨 渐变色主题

## 快速开始

### 1. 前置要求

- Node.js 18+
- npm 或 yarn
- Supabase 账号

### 2. 克隆项目

```bash
git clone https://github.com/netmanfisher/xcm-checkin.git
cd xiaoxiami-checkin
```

### 3. 安装依赖

```bash
npm install
```

### 4. 配置 Supabase

1. 在 [Supabase](https://supabase.com) 创建新项目
2. 在 SQL Editor 中执行 `database-schema.sql` 创建数据库表
3. 执行 `test-data.sql` 插入测试数据（可选）
4. 创建两个 Storage buckets：
   - `xcm_checkin_photos` - 打卡照片
   - `xcm_checkin_audio` - 打卡音频
5. 在项目设置中获取 API URL 和 anon key

### 5. 配置环境变量

复制 `.env.local` 文件，填入你的 Supabase 配置：

```env
VITE_SUPABASE_URL=your-supabase-url
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key
```

### 6. 启动开发服务器

```bash
npm run dev
```

访问 http://localhost:5173

## 默认账号

### 管理员
- 用户名: `admin`
- 密码: `admin123`

### 测试孩子
- 小虾米（公主主题）
- 小明（王子主题）

## 数据库表结构

所有表都以 `xcm_` 前缀命名，便于在多项目中隔离：

- `xcm_children` - 孩子信息
- `xcm_study_plans` - 学习计划
- `xcm_check_ins` - 打卡记录
- `xcm_stars_history` - 星币历史
- `xcm_wishes` - 愿望清单
- `xcm_redemptions` - 兑换记录
- `xcm_achievements` - 成就
- `xcm_user_achievements` - 用户成就
- `xcm_streaks` - 连续打卡
- `xcm_admins` - 管理员

## 部署

### Vercel 部署

1. Fork 本仓库
2. 在 [Vercel](https://vercel.com) 导入项目
3. 配置环境变量
4. 部署完成

## 项目结构

```
src/
├── api.js              # 旧版 API（知晓云，已废弃）
├── baas.js             # 旧版 BaaS（知晓云，已废弃）
├── main.js             # 应用入口
├── App.vue             # 根组件
├── supabase.js         # Supabase 客户端
├── router/
│   └── index.js        # 路由配置
├── stores/
│   └── auth.js         # 认证状态管理
└── views/
    ├── SelectChildView.vue      # 选择孩子
    ├── AdminLoginView.vue       # 管理员登录
    ├── AdminDashboardView.vue   # 管理后台首页
    ├── AdminReviewView.vue      # 打卡审核
    ├── HomeView.vue             # 孩子主页
    ├── CheckInView.vue          # 打卡详情
    ├── HonorsView.vue           # 荣誉墙
    ├── WishesView.vue           # 愿望清单
    ├── HistoryView.vue          # 历史记录
    └── ReportView.vue           # 月度报告
```

## 开发计划

查看 [开发文档.md](./开发文档.md) 了解详细设计。

## 许可证

MIT
